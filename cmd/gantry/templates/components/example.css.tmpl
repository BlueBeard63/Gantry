/* Styles for the example component. Scope under the class the
   component's root element renders. */

.example-card {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px 12px;
  border: 1px solid var(--gantry-border);
  border-radius: var(--gantry-radius);
}
