// Synthesized by gantry build - regenerated every run, never hand-edited.
#include "GantryShim.h"

// Stub: no Go server is linked into the scaffold yet. When c-archive
// linking lands this will call the archive's exported start function
// instead - see GantryShim.h for the plan.
int gantry_start(void) {
	return -1;
}
